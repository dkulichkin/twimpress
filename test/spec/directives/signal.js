// Generated by CoffeeScript 1.6.3
(function() {
  'use strict';
  describe('Directive: signal', function() {
    var scope;
    beforeEach(module('twimpressApp'));
    scope = {};
    beforeEach(inject(function($controller, $rootScope) {
      return scope = $rootScope.$new();
    }));
    return it('should make hidden element visible', inject(function($compile) {
      var element;
      element = angular.element('<signal></signal>');
      element = $compile(element)(scope);
      return expect(element.text()).toBe('this is the signal directive');
    }));
  });

}).call(this);

/*
//@ sourceMappingURL=signal.map
*/
